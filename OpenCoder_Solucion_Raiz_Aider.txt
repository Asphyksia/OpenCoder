================================================================================
     OPENCODER: SOLUCIÓN DE RAÍZ - INTEGRACIÓN AIDER + OPENGPU
================================================================================

Autor: Análisis de Arquitectura
Fecha: Enero 2025
Propósito: Documentar la solución DEFINITIVA para integrar Aider con OpenGPU
           en OpenCoder, resolviendo el problema de Python 3.13.

================================================================================
                           1. DIAGNÓSTICO DEL PROBLEMA
================================================================================

ESTADO ACTUAL:
❌ SimpleAgentEngine = Solo chat básico (NO es agentic)
❌ Aider no funciona con Python 3.13 (requiere 3.9-3.12)
❌ El proyecto está "estancado" como un chatbot sin capacidades de edición

CAUSA RAÍZ:
El problema NO es la integración con OpenGPU (eso ya funciona).
El problema es que Aider requiere Python 3.9-3.12 y tu sistema usa Python 3.13.

SOLUCIÓN OFICIAL DE AIDER:
Aider proporciona métodos para instalar en entorno aislado con Python 3.12.

================================================================================
                    2. SOLUCIÓN OFICIAL: AIDER-INSTALL
================================================================================

Aider documenta oficialmente (https://aider.chat/docs/install.html):

"If you already have python 3.8-3.13 installed, you can get started quickly...
aider-install will also install a separate version of python 3.12 to use with 
aider."

MÉTODO RECOMENDADO:
-------------------

# Paso 1: Instalar aider-install
python -m pip install aider-install

# Paso 2: Ejecutar aider-install (instala Python 3.12 + Aider en entorno aislado)
aider-install

# Paso 3: Usar Aider normalmente
aider --model openai/gpt-4o --message "Add hello function" test.py

Esto crea un entorno aislado donde Aider corre con Python 3.12, independientemente
del Python del sistema (3.13).

================================================================================
                    3. ARQUITECTURA PROPUESTA
================================================================================

┌─────────────────────────────────────────────────────────────────────────────┐
│                         ARQUITECTURA OPENCODER                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    FASTAPI BACKEND (Python 3.13)                    │   │
│  │                                                                     │   │
│  │  ┌─────────────────┐    ┌─────────────────┐    ┌────────────────┐  │   │
│  │  │   API Routes    │    │  OpenGPUAdapter │    │  Session Store │  │   │
│  │  │   /chat         │    │  (ya funciona)  │    │  (memoria)     │  │   │
│  │  │   /status       │    │                 │    │                │  │   │
│  │  │   /models       │    │                 │    │                │  │   │
│  │  └────────┬────────┘    └────────┬────────┘    └────────────────┘  │   │
│  │           │                      │                                  │   │
│  │           └──────────┬───────────┘                                  │   │
│  │                      │                                              │   │
│  │                      ▼                                              │   │
│  │           ┌─────────────────────┐                                   │   │
│  │           │   AiderBridge       │                                   │   │
│  │           │   (subprocess)      │───────────────────────────────────┼───┤
│  │           │                     │                                   │   │
│  │           │  - Ejecuta Aider    │                                   │   │
│  │           │  - Captura output   │                                   │   │
│  │           │  - Parsea diffs     │                                   │   │
│  │           └──────────┬──────────┘                                   │   │
│  │                      │                                              │   │
│  └──────────────────────┼──────────────────────────────────────────────┘   │
│                         │                                                   │
│                         │ subprocess                                        │
│                         ▼                                                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │              AIDER CLI (Entorno Aislado Python 3.12)                │   │
│  │                                                                      │   │
│  │  - Instalado via aider-install                                      │   │
│  │  - Corre en su propio entorno                                        │   │
│  │  - Conecta a OpenGPU via OPENAI_API_BASE                            │   │
│  │  - Todas las capacidades de Aider disponibles                       │   │
│  │                                                                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    OPENGPU RELAY                                     │   │
│  │                                                                      │   │
│  │  URL: https://relaygpu.com/backend/openai/v1                        │   │
│  │  Modelos: Qwen/Qwen3-Coder, gpt-4o, deepseek-coder, etc.            │   │
│  │  Auth: Authorization: Bearer relay_sk_xxx                           │   │
│  │                                                                      │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

================================================================================
                    4. IMPLEMENTACIÓN: AIDERBRIDGE
================================================================================

El componente clave es AiderBridge, que actúa como puente entre el backend
Python 3.13 y Aider CLI en Python 3.12.

ARCHIVO: opencoder/core/aider_bridge.py
----------------------------------------

"""
AiderBridge - Puente entre FastAPI y Aider CLI

Este módulo proporciona una interfaz para ejecutar Aider CLI como subprocess,
permitiendo usar todas las capacidades de Aider desde un backend Python 3.13.

Aider se ejecuta en un entorno Python 3.12 aislado (instalado via aider-install).
"""

import os
import json
import asyncio
import tempfile
from pathlib import Path
from typing import Optional
from dataclasses import dataclass, field
from loguru import logger


@dataclass
class AiderResult:
    """Resultado de ejecutar Aider."""
    success: bool
    message: str
    output: str
    diffs: str = ""
    files_changed: list[str] = field(default_factory=list)
    error: Optional[str] = None


class AiderBridge:
    """
    Puente para ejecutar Aider CLI como subprocess.
    
    Requiere que Aider esté instalado via:
        python -m pip install aider-install
        aider-install
    
    Esto crea un entorno aislado con Python 3.12 donde Aider funciona correctamente.
    """
    
    def __init__(
        self,
        repo_path: str,
        model: str = "Qwen/Qwen3-Coder",
        api_key: Optional[str] = None,
        base_url: str = "https://relaygpu.com/backend/openai/v1",
    ):
        """
        Inicializa el puente de Aider.
        
        Args:
            repo_path: Ruta al repositorio de código.
            model: Modelo a usar (sin prefijo openai/ para OpenGPU).
            api_key: API key de OpenGPU. Si no se proporciona, usa OPENGPU_API_KEY.
            base_url: URL base de OpenGPU.
        """
        self.repo_path = Path(repo_path).resolve()
        self.model = model
        self.api_key = api_key or os.getenv("OPENGPU_API_KEY", "")
        self.base_url = base_url
        
        # Verificar que el directorio existe
        if not self.repo_path.exists():
            self.repo_path.mkdir(parents=True, exist_ok=True)
        
        logger.info(f"[AiderBridge] Inicializado: repo={self.repo_path}, model={self.model}")
    
    def _build_env(self) -> dict:
        """Construye el entorno de variables para Aider."""
        env = os.environ.copy()
        
        # Configuración de OpenGPU para Aider
        env["OPENAI_API_KEY"] = self.api_key
        env["OPENAI_API_BASE"] = self.base_url
        
        # Para litellm (usado internamente por Aider)
        env["LITELLM_API_KEY"] = self.api_key
        
        return env
    
    def _find_aider_command(self) -> str:
        """Encuentra el comando de Aider instalado."""
        # Intentar encontrar aider en el PATH
        # aider-install crea un ejecutable 'aider'
        return "aider"
    
    async def execute(
        self,
        message: str,
        files: Optional[list[str]] = None,
        auto_commit: bool = True,
        dry_run: bool = False,
    ) -> AiderResult:
        """
        Ejecuta un comando de Aider.
        
        Args:
            message: Mensaje/instrucción para Aider.
            files: Lista de archivos a incluir (relativos al repo_path).
            auto_commit: Si True, hace commits automáticos.
            dry_run: Si True, no hace cambios reales.
        
        Returns:
            AiderResult con el resultado de la ejecución.
        """
        # Construir comando
        cmd = [
            self._find_aider_command(),
            f"--model=openai/{self.model}",  # Prefix openai/ para usar OPENAI_API_BASE
            f"--message={message}",
            "--yes",  # Auto-confirmar
            "--no-stream",  # Output completo al final
        ]
        
        if auto_commit:
            cmd.append("--auto-commits")
        else:
            cmd.append("--no-auto-commits")
        
        if dry_run:
            cmd.append("--dry-run")
        
        # Añadir archivos
        if files:
            for f in files:
                file_path = self.repo_path / f
                if file_path.exists():
                    cmd.append(str(file_path))
        else:
            # Si no hay archivos específicos, añadir todos los .py del repo
            py_files = list(self.repo_path.glob("*.py"))[:10]
            for f in py_files:
                cmd.append(str(f))
        
        logger.info(f"[AiderBridge] Ejecutando: {' '.join(cmd[:5])}...")
        
        try:
            # Ejecutar subprocess
            process = await asyncio.create_subprocess_exec(
                *cmd,
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
                cwd=str(self.repo_path),
                env=self._build_env(),
            )
            
            stdout, stderr = await process.communicate()
            
            output = stdout.decode("utf-8", errors="replace")
            error_output = stderr.decode("utf-8", errors="replace")
            
            # Parsear resultado
            success = process.returncode == 0
            
            # Extraer diffs del output
            diffs = self._extract_diffs(output)
            
            # Extraer archivos modificados
            files_changed = self._extract_files_changed(output)
            
            if success:
                logger.info(f"[AiderBridge] Éxito: {len(files_changed)} archivos modificados")
                return AiderResult(
                    success=True,
                    message="Comando ejecutado correctamente",
                    output=output,
                    diffs=diffs,
                    files_changed=files_changed,
                    error=None
                )
            else:
                logger.error(f"[AiderBridge] Error: {error_output}")
                return AiderResult(
                    success=False,
                    message="Error en Aider",
                    output=output,
                    diffs=diffs,
                    files_changed=files_changed,
                    error=error_output
                )
                
        except FileNotFoundError:
            error_msg = "Aider no encontrado. Instala con: python -m pip install aider-install && aider-install"
            logger.error(f"[AiderBridge] {error_msg}")
            return AiderResult(
                success=False,
                message=error_msg,
                output="",
                error=error_msg
            )
        except Exception as e:
            logger.error(f"[AiderBridge] Excepción: {e}")
            return AiderResult(
                success=False,
                message=f"Error inesperado: {str(e)}",
                output="",
                error=str(e)
            )
    
    def _extract_diffs(self, output: str) -> str:
        """Extrae los diffs del output de Aider."""
        diffs = []
        in_diff = False
        current_diff = []
        
        for line in output.split("\n"):
            if line.startswith("diff --git"):
                if current_diff:
                    diffs.append("\n".join(current_diff))
                current_diff = [line]
                in_diff = True
            elif in_diff:
                if line.startswith("diff --git"):
                    diffs.append("\n".join(current_diff))
                    current_diff = [line]
                else:
                    current_diff.append(line)
        
        if current_diff:
            diffs.append("\n".join(current_diff))
        
        return "\n\n".join(diffs)
    
    def _extract_files_changed(self, output: str) -> list[str]:
        """Extrae la lista de archivos modificados del output."""
        import re
        
        # Buscar patrones de archivos modificados
        patterns = [
            r"Applied edit to (.+)",
            r"Created (.+)",
            r"Modified (.+)",
            r"diff --git a/(.+) b/(.+)",
        ]
        
        files = set()
        for pattern in patterns:
            matches = re.findall(pattern, output)
            for match in matches:
                if isinstance(match, tuple):
                    files.update(match)
                else:
                    files.add(match)
        
        return list(files)
    
    async def add_files(self, files: list[str]) -> AiderResult:
        """
        Añade archivos al contexto de Aider sin ejecutar comando.
        
        Args:
            files: Lista de archivos a añadir.
        
        Returns:
            AiderResult indicando éxito.
        """
        # Construir comando para añadir archivos
        cmd = [self._find_aider_command(), "--message=/add"]
        cmd.extend(files)
        
        try:
            process = await asyncio.create_subprocess_exec(
                *cmd,
                stdout=asyncio.subprocess.PIPE,
                stderr=asyncio.subprocess.PIPE,
                cwd=str(self.repo_path),
                env=self._build_env(),
            )
            
            stdout, stderr = await process.communicate()
            
            return AiderResult(
                success=process.returncode == 0,
                message="Archivos añadidos" if process.returncode == 0 else "Error añadiendo archivos",
                output=stdout.decode("utf-8", errors="replace"),
                error=stderr.decode("utf-8", errors="replace") if process.returncode != 0 else None
            )
        except Exception as e:
            return AiderResult(
                success=False,
                message=f"Error: {str(e)}",
                output="",
                error=str(e)
            )
    
    async def get_status(self) -> dict:
        """Obtiene el estado del repositorio."""
        return {
            "repo_path": str(self.repo_path),
            "model": self.model,
            "api_configured": bool(self.api_key),
            "base_url": self.base_url,
        }


# ============================================================================
# FACTORY FUNCTION
# ============================================================================

def create_aider_bridge(
    repo_path: str,
    model: str = "Qwen/Qwen3-Coder",
    api_key: Optional[str] = None,
) -> AiderBridge:
    """
    Factory function para crear un AiderBridge.
    
    Args:
        repo_path: Ruta al repositorio.
        model: Modelo a usar.
        api_key: API key de OpenGPU.
    
    Returns:
        Instancia de AiderBridge configurada.
    """
    return AiderBridge(
        repo_path=repo_path,
        model=model,
        api_key=api_key,
    )

================================================================================
                    5. ACTUALIZACIÓN DE MAIN.PY
================================================================================

Actualizar opencoder/api/main.py para usar AiderBridge:

CAMBIOS NECESARIOS:

1. Importar AiderBridge:

from opencoder.core.aider_bridge import AiderBridge, AiderResult

2. Modificar AgentState:

class AgentState:
    def __init__(self):
        self.bridges: Dict[str, AiderBridge] = {}  # Cambiar engines -> bridges
        self.adapter: Optional[OpenGPUAdapter] = None
    
    def get_or_create_bridge(
        self,
        session_id: str,
        repo_path: str,
        model_name: str,
    ) -> AiderBridge:
        if session_id not in self.bridges:
            self.bridges[session_id] = AiderBridge(
                repo_path=repo_path,
                model=model_name,
            )
        return self.bridges[session_id]

3. Actualizar endpoint /chat:

@app.post("/chat", response_model=ChatResponse, tags=["Chat"])
async def chat(request: ChatRequest, session_id: Optional[str] = None):
    if not session_id:
        session_id = str(uuid.uuid4())
    
    repo_path = get_repo_path()
    model_name = request.model or os.getenv("OPENGPU_MODEL", "Qwen/Qwen3-Coder")
    
    # Usar AiderBridge en lugar de SimpleAgentEngine
    bridge = agent_state.get_or_create_bridge(
        session_id=session_id,
        repo_path=repo_path,
        model_name=model_name,
    )
    
    # Ejecutar con Aider
    result = await bridge.execute(request.message)
    
    return ChatResponse(
        success=result.success,
        message=result.message,
        events=[
            EventSchema(
                event_type="aider_output",
                content=result.output[:500],  # Limitar output
                timestamp=""
            )
        ],
        file_changes=[
            FileChangeSchema(
                filename=f,
                diff="",
                operation="modified"
            )
            for f in result.files_changed
        ],
        diffs=result.diffs,
        error=result.error
    )

================================================================================
                    6. INSTALACIÓN DE AIDER
================================================================================

PASOS DE INSTALACIÓN EN EL SERVIDOR:
------------------------------------

# Opción 1: Usar aider-install (RECOMENDADO)
python -m pip install aider-install
aider-install

# Opción 2: Usar uv
python -m pip install uv
uv tool install --force --python python3.12 --with pip aider-chat@latest

# Opción 3: Usar pipx
python -m pip install pipx
pipx install aider-chat --python python3.12

VERIFICAR INSTALACIÓN:
---------------------

aider --version
# Debe mostrar la versión de Aider

aider --help
# Debe mostrar la ayuda de Aider

================================================================================
                    7. CONFIGURACIÓN DE VARIABLES DE ENTORNO
================================================================================

Archivo .env:

# OpenGPU Configuration
OPENGPU_API_KEY=relay_sk_your_api_key_here
OPENGPU_BASE_URL=https://relaygpu.com/backend/openai/v1
OPENGPU_MODEL=Qwen/Qwen3-Coder

# OpenCoder Configuration
OPENCODER_REPO_PATH=./workspace
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# Aider Configuration (automático desde OPENGPU_*)
# Las variables se pasan al subprocess de Aider

================================================================================
                    8. DOCKERFILE ACTUALIZADO (OPCIONAL)
================================================================================

Si prefieres usar Docker para garantizar Python 3.12:

# Dockerfile.backend
FROM python:3.12-slim

WORKDIR /app

# Instalar aider
RUN pip install aider-install && aider-install

# Instalar dependencias del proyecto
COPY requirements.txt .
RUN pip install -r requirements.txt

# Copiar código
COPY . .

# Variables de entorno
ENV OPENAI_API_BASE=https://relaygpu.com/backend/openai/v1

CMD ["uvicorn", "opencoder.api.main:app", "--host", "0.0.0.0", "--port", "8000"]

================================================================================
                    9. CHECKLIST DE IMPLEMENTACIÓN
================================================================================

[ ] 1. Instalar aider-install en el servidor
        python -m pip install aider-install
        aider-install

[ ] 2. Verificar que Aider funciona
        aider --version

[ ] 3. Crear archivo opencoder/core/aider_bridge.py
        (Usar el código de la sección 4)

[ ] 4. Actualizar opencoder/api/main.py
        (Usar los cambios de la sección 5)

[ ] 5. Actualizar requirements.txt
        Añadir: aider-install (opcional, solo para documentación)

[ ] 6. Configurar variables de entorno
        (Usar .env de la sección 7)

[ ] 7. Probar la integración
        curl -X POST http://localhost:8000/chat \
          -H "Content-Type: application/json" \
          -d '{"message": "Create a hello.py file with a hello_world function"}'

[ ] 8. Verificar que los archivos se crean/modifican
        ls workspace/

================================================================================
                    10. VENTAJAS DE ESTA SOLUCIÓN
================================================================================

✅ USA AIDER COMPLETO:
   - Todas las capacidades de edición de código
   - Git integration automática
   - Diffs en formato estándar
   - Prompt engineering optimizado para código

✅ SIN REESCRIBIR CÓDIGO:
   - Aider ya tiene todo implementado
   - Solo necesitamos el bridge (wrapper)

✅ ENTORNO AISLADO:
   - Python 3.12 para Aider
   - Python 3.13 para el resto del proyecto
   - Sin conflictos de dependencias

✅ MANTENIBLE:
   - Actualizaciones de Aider automáticas
   - Código mínimo propio que mantener

✅ ESCALABLE:
   - Fácil añadir más features de Aider
   - Soporte para múltiples repositorios
   - Sesiones persistentes

================================================================================
                    11. ALTERNATIVAS CONSIDERADAS
================================================================================

❌ Implementar edición de código desde cero:
   - Mucho trabajo
   - Difícil de igualar la calidad de Aider
   - Mantenimiento continuo

❌ Usar Cursor/Claude Code:
   - No es open source
   - No se integra fácilmente con OpenGPU
   - Dependencia de terceros

❌ Downgrade a Python 3.12:
   - Puede romper otras dependencias
   - Limita el proyecto

✅ AiderBridge (SOLUCIÓN ELEGIDA):
   - Lo mejor de ambos mundos
   - Usa Aider completo
   - Mantiene Python 3.13 para el resto

================================================================================
                    12. NOTAS FINALES
================================================================================

1. Esta solución usa Aider COMO FUE DISEÑADO - como CLI tool.

2. El bridge es un wrapper simple que:
   - Configura las variables de entorno
   - Ejecuta Aider como subprocess
   - Parsea el output

3. OpenGPU funciona correctamente con Aider porque:
   - Acepta Authorization: Bearer
   - URL correcta: https://relaygpu.com/backend/openai/v1
   - Modelos sin prefijo: Qwen/Qwen3-Coder

4. El modelo se pasa con prefix "openai/" a Aider para que use
   OPENAI_API_BASE en lugar de api.openai.com.

================================================================================
                           FIN DEL DOCUMENTO
================================================================================
