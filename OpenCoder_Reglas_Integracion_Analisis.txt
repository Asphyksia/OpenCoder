================================================================================
     ANÁLISIS DE REGLAS DE INTEGRACIÓN AIDER + OPENGPU RELAY
================================================================================

Fecha: 2026-03-01
Proyecto: OpenCoder
API Key probada: relay_sk_a175218c...6360

================================================================================
1. NORMALIZACIÓN DE MODELOS
================================================================================

REGLA:
- Si el modelo es `openai/*`, `anthropic/*` u `ollama/*`, remover prefijo.
- Si el modelo tiene prefijo vendor no estándar (ej: `Qwen/Qwen3-Coder`), NO dividir.

ESTADO: ⚠️ PARCIALMENTE IMPLEMENTADO

✅ opengpu_adapter.py - CORRECTO:
   known_providers = ["openai", "anthropic", "ollama"]
   if potential_provider in known_providers:
       provider, model_name = model.split("/", 1)
   else:
       # Keep model as-is

❌ litellm_patch.py - FALTA NORMALIZACIÓN:
   clean_model = model  # ← NO limpia el prefijo openai/
   
   PROBLEMA: Si se pasa "openai/Qwen/Qwen3-Coder", lo envía tal cual a OpenGPU
   y falla con 403: "Model 'openai/Qwen/Qwen3-Coder' is not currently available"

ACCIÓN REQUERIDA:
   En litellm_patch.py, agregar:
   
   def _normalize_model_name(model: str) -> str:
       """Remove known provider prefixes from model name."""
       known_providers = ["openai", "anthropic", "ollama"]
       if "/" in model:
           potential_provider = model.split("/")[0].lower()
           if potential_provider in known_providers:
               return model.split("/", 1)[1]
       return model

================================================================================
2. COHERENCIA ENTRE RUTAS DE EJECUCIÓN
================================================================================

REGLA:
- Mantener la misma lógica de modelo entre AiderBridge y OpenGPUAdapter.

ESTADO: ❌ INCOHERENTE

AiderBridge (aider_bridge.py línea 151-155):
   if "/" in self.model:
       model_to_use = self.model.split("/", 1)[1]  # SIEMPRE divide
   else:
       model_to_use = self.model

OpenGPUAdapter (opengpu_adapter.py línea 162-175):
   known_providers = ["openai", "anthropic", "ollama"]
   if potential_provider in known_providers:
       # Solo divide si es proveedor conocido
   else:
       # NO divide

PROBLEMA:
   - AiderBridge divide "Qwen/Qwen3-Coder" → "Qwen3-Coder" ❌ INCORRECTO
   - OpenGPUAdapter mantiene "Qwen/Qwen3-Coder" → "Qwen/Qwen3-Coder" ✅ CORRECTO

ACCIÓN REQUERIDA:
   Alinear AiderBridge con la lógica de OpenGPUAdapter.

================================================================================
3. VALIDACIÓN ANTES DE ENTREGAR
================================================================================

REGLA:
- Añadir pruebas unitarias de normalización.

ESTADO: ❌ NO HAY TESTS

Tests requeridos:
   ✓ "openai/gpt-5.2" -> "gpt-5.2"
   ✓ "anthropic/claude-sonnet-4-6" -> "claude-sonnet-4-6"
   ✓ "ollama/llama3.2:3b" -> "llama3.2:3b"
   ✓ "Qwen/Qwen3-Coder" -> "Qwen/Qwen3-Coder"
   ✓ "gpt-5.2" -> "gpt-5.2"

ACCIÓN REQUERIDA:
   Crear tests/unit/test_model_normalization.py

================================================================================
4. ROBUSTEZ DE SUBPROCESS
================================================================================

REGLA:
- Registrar comando sin exponer API keys.
- Devolver errores accionables.
- Evitar fallbacks silenciosos.

ESTADO: ✅ MAYORMENTE IMPLEMENTADO

✅ AiderBridge registra comando de forma segura:
   logger.info(f"Executing: {' '.join(cmd[:4])}...")

✅ Devuelve errores estructurados:
   return AiderResult(
       success=False,
       error=error_output or "Unknown error"
   )

⚠️ MEJORA SUGERIDA:
   - Añadir causa probable en errores comunes
   - Ej: "Model not found" → "Check if model name is correct for OpenGPU"

================================================================================
5. CONTRATO API
================================================================================

REGLA:
- El endpoint /chat debe devolver errores claros.
- No truncar información crítica de error.

ESTADO: ✅ IMPLEMENTADO

✅ main.py devuelve errores claros:
   raise HTTPException(
       status_code=status.HTTP_500_INTERNAL_SERVER_ERROR,
       detail=f"Agent execution failed: {str(e)}"
   )

⚠️ MEJORA SUGERIDA:
   - Validar modelo antes de ejecutar
   - Devolver lista de modelos válidos en el error

================================================================================
6. SEGURIDAD Y CONFIGURACIÓN
================================================================================

REGLA:
- Nunca hardcodear claves ni rutas sensibles.
- Leer credenciales solo desde variables de entorno.
- Mantener compatibilidad con Python 3.10-3.12 para Aider.

ESTADO: ⚠️ PARCIALMENTE IMPLEMENTADO

❌ RUTAS HARDCODEADAS:

   agent_engine.py línea 57-58:
   _python12_site_packages = '/home/asphyksia/OpenCoder/venv/lib/python3.12/site-packages'
   
   agent_engine.py línea 803-806:
   wrapper_script = '/home/asphyksia/OpenCoder/scripts/aider_wrapper.py'
   python_exe = '/home/asphyksia/OpenCoder/venv/bin/python'

   aider_bridge.py línea 108:
   uv_path = shutil.which("uv") or "/home/asphyksia/.var/app/com.vscodium.codium/..."

✅ CREDENCIALES DESDE ENV:
   api_key = os.getenv("OPENGPU_API_KEY", "")

ACCIÓN REQUERIDA:
   Usar variables de entorno o detección dinámica:
   PROJECT_ROOT = Path(__file__).parent.parent.parent

================================================================================
7. CRITERIO DE CAMBIOS
================================================================================

REGLA:
- Cambios pequeños, atómicos y con commit message claro.
- Cada cambio en routing/model parsing debe incluir test.

ESTADO: ⚠️ PENDIENTE

No hay tests unitarios para model parsing.

================================================================================
RESUMEN DE ACCIONES REQUERIDAS
================================================================================

CRÍTICO (afecta funcionamiento):
1. [ ] Agregar normalización en litellm_patch.py
2. [ ] Corregir AiderBridge._find_aider_command() para usar misma lógica

IMPORTANTE (mejora calidad):
3. [ ] Crear tests/unit/test_model_normalization.py
4. [ ] Eliminar rutas hardcodeadas

RECOMENDADO:
5. [ ] Mejorar mensajes de error con causas probables
6. [ ] Validar modelo antes de ejecutar

================================================================================
PRUEBAS REALIZADAS CON API KEY
================================================================================

✅ OpenGPU Relay conexión: OK
✅ Modelos disponibles: 22 text-to-text models
✅ Chat completion Qwen/Qwen3-Coder: OK
❌ Chat completion openai/Qwen/Qwen3-Coder: FALLA (falta normalización)

================================================================================
FIN DEL ANÁLISIS
================================================================================
