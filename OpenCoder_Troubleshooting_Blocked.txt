================================================================================
     OPENCODER: TROUBLESHOOTING - "Your request was blocked"
================================================================================

PROBLEMA: Error "Your request was blocked" de OpenGPU Relay
ESTADO: API key activa, créditos disponibles, sin problemas en dashboard

================================================================================
                    PASO 1: VERIFICAR CON CURL DIRECTO
================================================================================

Ejecuta estos comandos desde la terminal para aislar el problema:

TEST 1: URL del backend directo (relaygpu.com)
------------------------------------------------
curl -v -X POST https://relaygpu.com/backend/openai/v1/chat/completions \
  -H "Authorization: Bearer TU_API_KEY_AQUI" \
  -H "Content-Type: application/json" \
  -d '{"model": "Qwen/Qwen3-Coder", "messages": [{"role": "user", "content": "Hello"}], "max_tokens": 50}'

TEST 2: URL alternativa (relay.opengpu.network)
------------------------------------------------
curl -v -X POST https://relay.opengpu.network/v1/chat/completions \
  -H "Authorization: Bearer TU_API_KEY_AQUI" \
  -H "Content-Type: application/json" \
  -d '{"model": "Qwen/Qwen3-Coder", "messages": [{"role": "user", "content": "Hello"}], "max_tokens": 50}'

TEST 3: Con X-API-Key header
-----------------------------
curl -v -X POST https://relay.opengpu.network/v2/openai/v1/chat/completions \
  -H "X-API-Key: TU_API_KEY_AQUI" \
  -H "Content-Type: application/json" \
  -d '{"model": "Qwen/Qwen3-Coder", "messages": [{"role": "user", "content": "Hello"}], "max_tokens": 50}'

ANOTA LOS RESULTADOS:
- ¿Cuál funciona?
- ¿Qué código HTTP devuelve?
- ¿Cuál es el mensaje de error exacto?

================================================================================
                    PASO 2: VERIFICAR MODELO DISPONIBLE
================================================================================

TEST: Listar modelos disponibles
---------------------------------
curl -s https://relay.opengpu.network/v2/models | python3 -m json.tool

O alternativamente:
curl -s https://relaygpu.com/v2/models | python3 -m json.tool

BUSCA:
- El modelo exacto que quieres usar (Qwen/Qwen3-Coder, gpt-4o, etc.)
- El formato del nombre (con o sin provider prefix)
- La categoría (auto, direct, opengpu)

================================================================================
                    PASO 3: VERIFICAR FORMATO DEL REQUEST
================================================================================

En aider_bridge.py, el modelo se pasa así:

    cmd.extend([
        f"--model=openai/{self.model}",  # Prefix openai/ añadido
        ...
    ])

SI self.model = "Qwen/Qwen3-Coder"
ENTOCES Aider intenta: model="openai/Qwen/Qwen3-Coder"

Esto puede ser incorrecto. PRUEBA estas variantes:

VARIANTE A: Modelo sin prefijo en .env
---------------------------------------
OPENGPU_MODEL=Qwen/Qwen3-Coder
# Y el código añade: --model=openai/Qwen/Qwen3-Coder

VARIANTE B: Modelo con formato provider/model
----------------------------------------------
OPENGPU_MODEL=openai/Qwen/Qwen3-Coder
# Y el código NO añade prefix

VARIANTE C: Modelo simple
-------------------------
OPENGPU_MODEL=gpt-4o
# El código añade: --model=openai/gpt-4o

================================================================================
                    PASO 4: EJECUTAR AIDER MANUALMENTE
================================================================================

Ejecuta Aider directamente desde la terminal para ver el error completo:

# Configurar variables
export OPENAI_API_KEY="TU_API_KEY"
export OPENAI_API_BASE="https://relaygpu.com/backend/openai/v1"

# Ejecutar Aider con debug
aider --model=openai/Qwen/Qwen3-Coder \
      --message "Say hello" \
      --yes \
      --no-stream \
      --verbose \
      test.py

OBSERVA:
- ¿Qué URL intenta conectar?
- ¿Qué headers envía?
- ¿Cuál es el error exacto?

================================================================================
                    PASO 5: VERIFICAR RED/FIREWALL
================================================================================

El error "Your request was blocked" puede venir de:

1. Cloudflare/WAF de OpenGPU bloqueando ciertos User-Agents
2. IP bloqueada por uso excesivo
3. VPN/Proxy interferiendo

TEST: Verificar respuesta de los endpoints
-------------------------------------------
curl -I https://relaygpu.com/backend/openai/v1/
curl -I https://relay.opengpu.network/v1/

¿Qué headers de respuesta ves?
- ¿Hay Cloudflare?
- ¿Hay rate limiting headers?

================================================================================
                    PASO 6: ALTERNATIVAS SI NADA FUNCIONA
================================================================================

OPCIÓN A: Usar X-API-Key header en lugar de Bearer
----------------------------------------------------
Modificar aider_bridge.py para usar X-API-Key:

    # Aider no soporta X-API-Key directamente, pero podemos crear
    # un adapter personalizado

OPCIÓN B: Crear un proxy local
-------------------------------
Crear un servidor local que:
1. Reciba requests de Aider
2. Añada X-API-Key header
3. Reenvíe a OpenGPU

OPCIÓN C: Usar OpenGPUAdapter directamente (ya funciona)
---------------------------------------------------------
Volver a usar SimpleAgentEngine con OpenGPUAdapter para chat,
y manejar la edición de archivos de otra forma.

================================================================================
                    PASO 7: LOGS DETALLADOS
================================================================================

Modifica aider_bridge.py temporalmente para capturar más info:

    async def execute(self, message: str, files: Optional[List[str]] = None) -> AiderResult:
        # ... código existente ...
        
        # AÑADIR: Capturar stderr completo
        output = stdout.decode("utf-8", errors="replace")
        error_output = stderr.decode("utf-8", errors="replace")
        
        # AÑADIR: Log del environment
        logger.info(f"[AiderBridge] ENV: OPENAI_API_BASE={self._build_env().get('OPENAI_API_BASE')}")
        logger.info(f"[AiderBridge] ENV: OPENAI_API_KEY={self.api_key[:10]}...")
        logger.info(f"[AiderBridge] Command: {cmd}")
        logger.info(f"[AiderBridge] Return code: {process.returncode}")
        logger.info(f"[AiderBridge] STDOUT: {output[:500]}")
        logger.info(f"[AiderBridge] STDERR: {error_output[:500]}")

================================================================================
                    RESULTADOS A REPORTAR
================================================================================

Por favor, ejecuta los tests anteriores y reporta:

1. [ ] Resultado de TEST 1 (curl a relaygpu.com)
2. [ ] Resultado de TEST 2 (curl a relay.opengpu.network)
3. [ ] Resultado de TEST 3 (X-API-Key)
4. [ ] Lista de modelos disponibles
5. [ ] Resultado de Aider manual
6. [ ] Headers de respuesta

Con esta información podremos identificar exactamente dónde está el problema.

================================================================================
